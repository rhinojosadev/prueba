Pasos iniciar git

1) git init

Pasos agregar archivo con nuevos cambios

1) git add -A
2) git commit -m "Aqui tu mensaje"
3) git push 


Pasos obtener informacion del repositorio remoto

1) git remote add origin $url.git ( esto solo se hace una vez);
2) git pull
